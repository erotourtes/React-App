{
  "name": "React-App",
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "start": "turbo run start:dev",
    "start:prod": "turbo run start:prod",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  }
}
